name: CI - Research and Search

on:
  push:
    branches: ["master", "dev"]
  pull_request:
    branches: ["master", "dev"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  research-search:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python and uv
        uses: ./.github/actions/setup-python-uv

      - name: Run research and search tests
        env:
          GLOBAL_MODEL_API_KEY: ${{ secrets.GLOBAL_MODEL_API_KEY }}
          GLOBAL_MODEL_BASE_URL: ${{ secrets.GLOBAL_MODEL_BASE_URL }}
          GLOBAL_MODEL_ID: ${{ secrets.GLOBAL_MODEL_ID }}
          EMBEDDING_MODEL: ${{ secrets.EMBEDDING_MODEL }}
          EMBEDDING_MODEL_API_KEY: ${{ secrets.EMBEDDING_MODEL_API_KEY }}
        run: >
          uv run pytest
          test/unit/agent/nodes/research
          test/unit/agent/nodes/query_generator
          test/unit/agent/nodes/search
          test/unit/agent/nodes/utils
          -v --asyncio-mode=strict
